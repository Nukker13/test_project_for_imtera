<?php

return [
    // Адрес Python-сервиса с парсером. Обращение идёт по сети, а не через
    // shell/CLI: сервис живёт в отдельном контейнере и масштабируется отдельно.
    'url' => env('PARSER_URL', 'http://python-service:8001'),

    // Один прогон — это десяток HTTP-запросов к Яндексу с паузами между ними,
    // поэтому таймаут измеряется минутами, а не секундами.
    'timeout' => (int) env('PARSER_TIMEOUT', 600),

    // Потолок выдачи самого Яндекса: ~12 страниц по 50 отзывов на одну
    // сортировку. Добор через другие сортировки не делается осознанно.
    'max_pages' => (int) env('YANDEX_MAX_PAGES', 12),
];
